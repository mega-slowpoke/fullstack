namespace June26;

public class ShortQuestion
{
    /*
     * 1. Generics are a form of polymorphism used to reduce
     *      highly duplicate code. A class or interface doesn't need
     *      to provide each data type with its own set of ad-hoc methods.
     *      Instead, a single set of methods can be applied to different types
     *      of variable
     * 2.  List<string> lst = new List<string>();
     * 3. Two: key and value
     * 4. True
     * 5. Add()
     * 6. Remove(), Clear()
     * 7. ClassName<T>
     * 8. False
     * 9. True
     * 10. True
    */ 
}